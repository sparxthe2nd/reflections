How did viewing a diff between two versions of a file help you see the bug that was introduced?
diff filters all unessesary stuff out and only shows lines that differs. That makes it easyer to locate
any errors.

How could having easy access to the entire history of a file make you a more efficient programmer 
in the long term?
I do not have to keep track of changes my self.

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, 
vs having versions automatically saved, like Google Docs does?
Pro = less clutter, Con = you might forget to commit

Why do you think some version control systems, like Git, allow saving multiple files in one commit, 
while others, like Google Docs, treat each file separately?
Because Git focuses on code where inter-dependend files are normal, 
while Google Docs is for general use.

How can you use the commands git log and git diff to view the history of files?
git log shows the different commits and git diff shows the difference between to commits

How might using version control make you more confident to make changes that could break something?
You can alwalys revert to other commits

Now that you have your workspace set up, what do you want to try using Git for?
I don't know